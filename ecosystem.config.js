module.exports = {
  apps: [
    {
      name: 'tictactoe-backend',
      script: 'npm',
      args: 'start', // Reemplaza 'start' con el script que deseas ejecutar
      log_date_format: 'YYYY-MM-DD HH:mm:ss', // Formato de fecha y hora
    },
  ],
};
